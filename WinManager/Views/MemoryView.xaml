<UserControl x:Class="WinManager.Views.MemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinManager.Views"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:WinManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="850">
    <d:UserControl.DataContext>
        <vm:MemoryControl> </vm:MemoryControl>
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a1a1aa"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="SubValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#d4d4d8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="5,0,0,2"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#27272a"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">

        <Grid Margin="10">
            <Border Background="#18181b" CornerRadius="12" BorderBrush="#27272a" BorderThickness="1" Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
                            <TextBlock Text="Memory" FontSize="28" FontWeight="ExtraBold" Foreground="White"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding TotalMemoryGB, StringFormat={}{0:N1} GB}" FontSize="16" Foreground="#f97316" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding FormFactor}" FontSize="16" Foreground="#a1a1aa" Margin="5,5,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding MemoryUsagePercent, StringFormat={}{0:N0} %}" 
                                       FontSize="42" FontWeight="Bold" Foreground="#f97316" 
                                       HorizontalAlignment="Right"/>
                            <TextBlock Text="MEMORY USAGE" FontSize="12" FontWeight="Bold" Foreground="#71717a" 
                                       HorizontalAlignment="Right" Margin="0,-5,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" Height="280" BorderBrush="#27272a" BorderThickness="1" CornerRadius="8" 
                            Background="#09090b" Margin="0,0,0,20" Padding="10">
                        <lvc:CartesianChart Series="{Binding Series}"
                                            XAxes="{Binding XAxes}"
                                            YAxes="{Binding YAxes}"
                                            TooltipPosition="Top"/>
                    </Border>

                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="IN USE (Compressed)" Style="{StaticResource LabelStyle}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding InUseGB, StringFormat={}{0:N1} GB}" Style="{StaticResource ValueStyle}" Foreground="#f97316"/>
                                    <TextBlock Text="{Binding InUseMB, StringFormat={}({0:N0} MB)}" Style="{StaticResource SubValueStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="AVAILABLE" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding AvailableGB, StringFormat={}{0:N1} GB}" Style="{StaticResource ValueStyle}" Foreground="#fb923c"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <GroupBox Grid.Row="3" Header="Technical Details" Foreground="#71717a" BorderBrush="#27272a" Margin="0,10,0,0">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Committed" Style="{StaticResource LabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding CommittedGB, StringFormat={}{0:N1}}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                        <TextBlock Text="/" Style="{StaticResource ValueStyle}" FontSize="16" Margin="2,0"/>
                                        <TextBlock Text="{Binding CommittedLimitGB, StringFormat={}{0:N1} GB}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Paged pool" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding PagedPoolMB, StringFormat={}{0:N0} MB}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Non-paged pool" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding NonPagedPoolMB, StringFormat={}{0:N0} MB}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Cached" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding CachedGB, StringFormat={}{0:N1} GB}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Hardware reserved" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding HardwareReservedMB, StringFormat={}{0:N0} MB}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Speed" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding MemorySpeed, StringFormat={}{0} MHz}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Slots used" Style="{StaticResource LabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding SlotsUsed}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                        <TextBlock Text=" of " Style="{StaticResource ValueStyle}" FontSize="16" Foreground="#a1a1aa"/>
                                        <TextBlock Text="{Binding TotalSlots}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Form factor" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding FormFactor}" Style="{StaticResource ValueStyle}" FontSize="16"/>
                                </StackPanel>
                            </StackPanel>

                        </Grid>
                    </GroupBox>

                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
