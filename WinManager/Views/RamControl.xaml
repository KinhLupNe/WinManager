<UserControl x:Class="WinManager.Views.RamControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinManager.ViewModels">
    <UserControl.DataContext>
        <vm:RamPresenter/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="RAM" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="{Binding RamUsageText}"
                       FontSize="24"
                       TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
