<UserControl x:Class="WinManager.Views.Components.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="200">
    <Border Background="#1AFFFFFF" CornerRadius="8" Padding="15,10" Margin="0,0,15,15" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="#88FFFFFF" FontSize="12" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</UserControl>