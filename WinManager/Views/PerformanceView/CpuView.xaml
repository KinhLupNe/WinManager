<UserControl x:Class="WinManager.Views.CPUView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WinManager.Views"
             xmlns:vm="clr-namespace:WinManager.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="850">

    <UserControl.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a1a1aa" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#27272a" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">

        <Grid Margin="10">
            <Border Background="#18181b" CornerRadius="12" BorderBrush="#27272a" BorderThickness="1" Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left">
                            <TextBlock Text="CPU" FontSize="28" FontWeight="ExtraBold" Foreground="White" />
                            <TextBlock Text="{Binding Name}" FontSize="16" Foreground="#06b6d4" Margin="0,5,0,0" />
                        </StackPanel>

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Usage, StringFormat={}{0:N0} %}"
                                       FontSize="42" FontWeight="Bold" Foreground="#06b6d4"
                                       HorizontalAlignment="Right" />
                            <TextBlock Text="% UTILIZATION" FontSize="12" FontWeight="Bold" Foreground="#71717a"
                                       HorizontalAlignment="Right" Margin="0,-5,0,0" />
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" Height="280" BorderBrush="#27272a" BorderThickness="1" CornerRadius="8"
                            Background="#09090b" Margin="0,0,0,20" Padding="10">
                        <lvc:CartesianChart Series="{Binding Series}"
                                            XAxes="{Binding XAxes}"
                                            YAxes="{Binding YAxes}"
                                            TooltipPosition="Top" />
                    </Border>

                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="UTILIZATION" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Usage, StringFormat={}{0:N1} %}" Style="{StaticResource ValueStyle}" Foreground="#06b6d4" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="SPEED" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Speed, StringFormat={}{0:N2} GHz}" Style="{StaticResource ValueStyle}" Foreground="#22d3ee" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="UPTIME" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding UpTime}" Style="{StaticResource ValueStyle}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <GroupBox Grid.Row="3" Header="Technical Specs" Foreground="#71717a" BorderBrush="#27272a" Margin="0,10,0,0">
                        <UniformGrid Columns="4" Margin="10">

                            <StackPanel Margin="10">
                                <TextBlock Text="Temperature" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Temperature}" Style="{StaticResource ValueStyle}" Foreground="#f87171" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Processes" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Processes}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Threads" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Threads}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Handles" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Handles}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Cores" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Core}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Voltage" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding Voltage}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>

                            <StackPanel Margin="10">
                                <TextBlock Text="Base Speed" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding BaseSpeed, StringFormat={}{0:N2} GHz}" Style="{StaticResource ValueStyle}" FontSize="16" />
                            </StackPanel>
                        </UniformGrid>
                    </GroupBox>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>