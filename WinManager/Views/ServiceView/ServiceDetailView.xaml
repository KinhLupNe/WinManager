<Window x:Class="WinManager.Views.ServiceDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Service Details" Height="450" Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ModernScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="Foreground" Value="#3f3f46" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="8" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbBorder"
                                                    Background="#3f3f46"
                                                    CornerRadius="4" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="#71717a" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="#a1a1aa" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource ModernScrollBarStyle}" />
    </Window.Resources>
    <Border Background="#18181b"
            CornerRadius="12"
            BorderBrush="#3f3f46"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" MouseDown="Header_MouseDown">
                <Grid Margin="20,0">
                    <TextBlock Text="Service Details"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="16"
                               VerticalAlignment="Center" />

                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Width="30" Height="30" Click="CloseBtn_Click"
                            Cursor="Hand" Background="Transparent" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                          Fill="#a1a1aa" Stretch="Uniform" Width="12" Height="12" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#ef4444" />
                                        <Setter Property="Cursor" Value="Hand" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="#27272a" />

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20">
                <StackPanel>
                    <TextBlock Text="{Binding DisplayName}" FontSize="22" FontWeight="Bold" Foreground="#10b981" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding ServiceName}" FontSize="14" Foreground="#71717a" Margin="0,2,0,20" />

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" Foreground="#a1a1aa" Width="100" Margin="0,0,0,10" />
                        <Border Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" CornerRadius="4" Background="#27272a" Padding="10,2" Margin="0,0,0,10">
                            <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" />
                        </Border>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Startup Type:" Foreground="#a1a1aa" Margin="0,0,0,10" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding StartupType}" Foreground="White" Margin="0,0,0,10" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Process ID:" Foreground="#a1a1aa" Margin="0,0,0,10" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Pid}" Foreground="White" Margin="0,0,0,10" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Path:" Foreground="#a1a1aa" Margin="0,0,0,10" />
                    </Grid>

                    <TextBlock Text="Description" Foreground="#a1a1aa" FontWeight="SemiBold" Margin="0,0,0,10" />
                    <Border Background="#27272a" CornerRadius="6" Padding="15">
                        <TextBlock Text="{Binding Description}"
                                   Foreground="#d4d4d8"
                                   TextWrapping="Wrap"
                                   LineHeight="22" />
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#09090b" CornerRadius="0,0,12,12" BorderBrush="#27272a" BorderThickness="0,1,0,0">
                <Button Content="Close"
                        Width="100" Height="35"
                        HorizontalAlignment="Right" Margin="0,0,20,0"
                        Click="CloseBtn_Click"
                        Foreground="White" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#27272a" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3f3f46" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>